#ifndef PID_CONTROLLER_H
#define PID_CONTROLLER_H

#include <stdint.h>

// ============================================================================
// КОНСТАНТЫ ПИД
// ============================================================================
#define PID_SCALE 1000

#define KP (1000)
#define KI (80)
#define KD (300)

// ============================================================================
// ВНЕШНИЕ ПЕРЕМЕННЫЕ ПИД
// ============================================================================
extern int32_t setpoint;    // Уставка (мВ)
extern int32_t feedback;    // Обратная связь (мВ)
extern int32_t error;       // Ошибка (мВ)
extern int32_t output;      // Выход (0-1000)

// ============================================================================
// ФУНКЦИИ ПРЕОБРАЗОВАНИЯ
// ============================================================================
static inline int32_t adc_to_millivolts(uint16_t adc_val) {
	return ((int32_t) adc_val * 3300L) / 4095L;
}

static inline int32_t setpoint_to_millivolts(uint16_t setpoint_1023) {
	return ((int32_t) setpoint_1023 * 3300L) / 1023L;
}

static inline uint16_t millivolts_to_plotter(int32_t mv) {
	return (uint16_t) ((mv * 1023L) / 3300L);
}

// ============================================================================
// ПИД ФУНКЦИИ
// ============================================================================
int32_t pid_update_mv(int32_t sp_mv, int32_t fb_mv);

#endif // PID_CONTROLLER_H
